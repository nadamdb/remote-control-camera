matrix:
    include:
        - language: python
          python:
              - "3.6"
          before_script:
              - cd raspberry
              - pip install -r requirements.txt
          script:
              - pytest

        - language: node_js
          node_js:
              - "11"
          before_script:
              - cd imageserver
              - npm i
          script:
              - npm test
              - npm run coverage
